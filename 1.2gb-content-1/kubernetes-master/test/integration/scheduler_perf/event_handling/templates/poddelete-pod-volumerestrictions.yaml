apiVersion: v1
kind: Pod
metadata:
  generateName: pod-unsched-
  labels:
    type: unsched
spec:
  containers:
  - image: registry.k8s.io/pause:3.10.1
    name: pause
  volumes:
  - name: vol
    persistentVolumeClaim:
      claimName: pvc-{{ .Index }}
# ID-1768294479-e6f6fedb
