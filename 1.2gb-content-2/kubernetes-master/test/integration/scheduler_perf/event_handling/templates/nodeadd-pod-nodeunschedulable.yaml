apiVersion: v1
kind: Pod
metadata:
  generateName: pod-unsched-
spec:
  tolerations:
  - key: allow
    operator: Exists
    effect: NoSchedule
  containers:
  - image: registry.k8s.io/pause:3.10
    name: pause
# ID-1768294466-5a88ff2a
